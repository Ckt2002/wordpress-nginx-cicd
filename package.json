{
    "name": "web_cicd",
    "version": "1.0.0",
    "description": "A description of your project",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node index.js",
        "build": "powershell -Command \"New-Item -ItemType Directory -Force -Path dist; Get-ChildItem -Path '.\\nginx\\html\\wordpress\\*' -File -Recurse | Where-Object { $_.Name -ne 'wp_config.php' } | ForEach-Object { Copy-Item -Path $_.FullName -Destination '.\\dist\\' -Force }\""
    },
    "keywords": [],
    "author": ""

}